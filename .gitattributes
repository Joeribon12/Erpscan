# Normaliseer line-endings: in de repo altijd LF, ongeacht OS.
* text=auto eol=lf

# Binaire bestanden ongemoeid laten (voor het geval je later assets toevoegt).
*.png binary
*.jpg binary
*.webp binary
*.woff2 binary
