{
  "name": "lead-capture",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker die scan-leads valideert en naar Google Sheets schrijft.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
